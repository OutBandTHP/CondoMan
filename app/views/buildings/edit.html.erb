<% provide(:title, 'Building') %>

<h2>
  Edit <%= "Building #{@building.id_code} of #{@project.name}"  if project_set? %>
</h2>

<div class='row'>
  <div class='col-md-6 col-md-offset-3'>
    <%= form_with(model: @building, local: true) do |f| %>
      <%= render 'form_building' %>
    <% end %>
  </div>
</div>
