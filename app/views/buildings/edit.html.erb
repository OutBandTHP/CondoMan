<% provide(:title, 'בניין') %>

<h2>
  <%= "בניין #{@building.id_code} בפרוייקט #{@project.name}"  if project_set? %>
</h2>

<div class='row'>
  <div class='col-md-6 col-md-offset-3'>
    <%= form_with(model: @building, local: true) do |f| %>
      <%= render 'form_building' %>
    <% end %>
  </div>
</div>
