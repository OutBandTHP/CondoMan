<% provide(:title, 'Building') %>

<h2>
  Edit Building <%= "#{@project.name}, Building #{@building.id_code}" %>
</h2>

<div class='row'>
  <div class='col-md-6 col-md-offset-3'>
    <%= form_with(model: @building, local: true) do |f| %>
      <%= render 'building' %>
    <% end %>
  </div>
</div>
