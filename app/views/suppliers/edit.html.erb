<% provide(:title, 'Supplier') %>

<h2>
  ספק <%= "#{@supplier.name} בפרוייקט #{@project.name}" %>
</h2>

<div class='row'>
  <div class='col-md-6 col-md-offset-3'>
    <%= form_with(model: @supplier, local: true) do |f| %>
      <%= render 'form' %>
    <% end %>
  </div>
</div>
