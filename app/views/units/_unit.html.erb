<tr id="unit-<%= unit.id %>">
  <td><%= link_to "#{unit.number}", edit_unit_path(unit.id) %></td>
  <td><%= unit.area %></td></td>
  <td><%= Building.find(unit.building).id_code %></td>
  <td><%= unit.floor %></td>
  <td><%= unit.since %></td>
</tr>
