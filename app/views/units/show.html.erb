<% provide(:title, 'Unit') %>

<h2>
  דירה <%= "#{@unit.number} בפרוייקט #{@project.name}"  if project_set? %>
</h2>

<div class='row'>
  <div class='col-md-6 col-md-offset-3'>
    <%= render 'form_unit' %>
  </div>
</div>  
