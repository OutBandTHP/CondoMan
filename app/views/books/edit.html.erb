<% provide(:title, 'Book') %>

<h2>
  ספר <%= "#{@book.name} בפרוייקט #{@project.name}"  if project_set? %>
</h2>

<div class='row'>
  <div class='col-md-6 col-md-offset-3'>
    <%= form_with(model: @book, local: true) do |f| %>
      <%= render 'form' %>
    <% end %>
  </div>
</div>
