<div class='center jumbotron'>
  <h1>ברוך הבא למערכת נהול חשבונות הוועד </h1>
  <h2>
    <% if logged_in? %>
      <%= current_user.email %>
    <% else %>
      <%= link_to 'הרשם עכשיו!', new_user_path, class: 'tn btn-lg btn-primary' %>
    <% end %>
  </h2>
  <br>
  <br>
  <% valid_notices.each do |tm| %>
    <li><%= tm.text_message %></li>
  <% end %>
</div>
